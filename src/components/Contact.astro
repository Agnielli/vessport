<section id="contacto" class="py-20 bg-white">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-16">
      <h2
        class="text-4xl md:text-5xl font-light text-gray-900 mb-6 tracking-tight"
      >
        Hablemos <span class="text-primary">de tu proyecto</span>
      </h2>
      <p
        class="text-xl text-gray-600 font-light leading-relaxed mx-auto max-w-3xl"
      >
        ¿Listo para crear tus prendas personalizadas Fullprint?
      </p>
    </div>

    <div class="grid grid-cols-1 sm:grid-cols-3 lg:grid-cols-5 gap-8 lg:gap-12">
      <div class="col-span-1 lg:col-span-2 space-y-12 py-8">
        <div>
          <h3
            class="text-2xl font-bold text-gray-900 mb-8 flex items-center gap-3"
          >
            <span class="w-8 h-1 bg-primary rounded-full"></span>
            Información de Contacto
          </h3>
          <p class="text-gray-500 font-light mb-8 -mt-4">
            Nuestro equipo de atención al cliente está listo para ayudarte con
            tu pedido personalizado.
          </p>

          <div class="space-y-8">
            <div
              class="group flex items-start space-x-5 p-4 rounded-2xl hover:bg-primary/5 transition-colors duration-300"
            >
              <div
                class="w-14 h-14 bg-white border border-gray-100 shadow-sm rounded-xl flex items-center justify-center flex-shrink-0 group-hover:bg-primary group-hover:text-white transition-all duration-300"
              >
                <svg
                  class="w-6 h-6 text-primary group-hover:text-white"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="1.5"
                    d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"
                  ></path>
                </svg>
              </div>
              <div>
                <h4 class="font-bold text-gray-900 mb-1">Teléfono</h4>
                <p class="text-gray-600 font-medium">+34 693 938 377</p>
                <p class="text-sm text-gray-400">
                  Lunes a Viernes: 8:00 - 18:00
                </p>
              </div>
            </div>

            <div
              class="group flex items-start space-x-5 p-4 rounded-2xl hover:bg-primary/5 transition-colors duration-300"
            >
              <div
                class="w-14 h-14 bg-white border border-gray-100 shadow-sm rounded-xl flex items-center justify-center flex-shrink-0 group-hover:bg-primary group-hover:text-white transition-all duration-300"
              >
                <svg
                  class="w-6 h-6 text-primary group-hover:text-white"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="1.5"
                    d="M3 8l7.89 4.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"
                  ></path>
                </svg>
              </div>
              <div>
                <h4 class="font-bold text-gray-900 mb-1">Email</h4>
                <p class="text-gray-600 font-medium">info@vessport.es</p>
                <p class="text-sm text-gray-400">
                  Respuesta garantizada en 24h
                </p>
              </div>
            </div>

            <div
              class="group flex items-start space-x-5 p-4 rounded-2xl hover:bg-primary/5 transition-colors duration-300"
            >
              <div
                class="w-14 h-14 bg-white border border-gray-100 shadow-sm rounded-xl flex items-center justify-center flex-shrink-0 group-hover:bg-primary group-hover:text-white transition-all duration-300"
              >
                <svg
                  class="w-6 h-6 text-primary group-hover:text-white"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="1.5"
                    d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"
                  ></path>
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="1.5"
                    d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
                </svg>
              </div>
              <div>
                <h4 class="font-bold text-gray-900 mb-1">Ubicación</h4>
                <p class="text-gray-600 font-medium">C/Mayor nº258.</p>
                <p class="text-sm text-gray-400">04630 Garrucha, España.</p>
              </div>
            </div>

            <div
              class="group flex items-start space-x-5 p-4 rounded-2xl hover:bg-green-50 transition-colors duration-300"
            >
              <div
                class="w-14 h-14 bg-white border border-green-50 shadow-sm rounded-xl flex items-center justify-center flex-shrink-0 group-hover:bg-green-500 group-hover:text-white transition-all duration-300"
              >
                <svg
                  class="w-7 h-7 text-green-500 group-hover:text-white"
                  fill="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893A11.821 11.821 0 0020.885 3.106"
                  ></path>
                </svg>
              </div>
              <div>
                <h4 class="font-bold text-gray-900 mb-1">WhatsApp</h4>
                <p class="text-gray-600 font-medium">+34 693 938 377</p>
                <a
                  href="https://wa.me/34693938377"
                  target="_blank"
                  class="inline-flex items-center text-green-600 hover:text-green-700 text-sm font-bold mt-1 group-hover:translate-x-1 transition-transform"
                >
                  Chatear ahora
                  <svg
                    class="w-4 h-4 ml-1"
                    fill="none"
                    stroke="currentColor"
                    viewBox="0 0 24 24"
                    ><path
                      stroke-linecap="round"
                      stroke-linejoin="round"
                      stroke-width="2"
                      d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"
                    ></path></svg
                  >
                </a>
              </div>
            </div>
          </div>
        </div>
      </div>

      <div class="col-span-1 sm:col-span-2 lg:col-span-3">
        <div
          class="bg-white p-8 md:p-10 rounded-3xl shadow-2xl border border-gray-100 relative overflow-hidden"
        >
          <div
            class="absolute top-0 right-0 w-32 h-32 bg-primary/5 rounded-full -mr-16 -mt-16"
          >
          </div>

          <h3 class="text-3xl font-bold text-gray-900 mb-2 relative">
            Envíanos un Mensaje
          </h3>
          <p class="text-gray-500 font-light mb-8 relative">
            Responderemos a tu solicitud lo antes posible.
          </p>

          <div id="form-wrapper" class="relative">
            <form id="contactForm" class="space-y-6">
              <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <div>
                  <label
                    for="name"
                    class="block text-xs font-bold text-gray-400 uppercase tracking-widest mb-2 ml-1"
                    >Nombre Completo *</label
                  >
                  <input
                    type="text"
                    id="name"
                    name="name"
                    required
                    class="w-full px-5 py-4 bg-gray-50/50 border border-gray-200 rounded-2xl focus:bg-white focus:ring-4 focus:ring-primary/10 focus:border-primary outline-none transition-all duration-300 placeholder:text-gray-400"
                    placeholder="Escribe tu nombre"
                  />
                </div>
                <div>
                  <label
                    for="email"
                    class="block text-xs font-bold text-gray-400 uppercase tracking-widest mb-2 ml-1"
                    >Email *</label
                  >
                  <input
                    type="email"
                    id="email"
                    name="email"
                    required
                    class="w-full px-5 py-4 bg-gray-50/50 border border-gray-200 rounded-2xl focus:bg-white focus:ring-4 focus:ring-primary/10 focus:border-primary outline-none transition-all duration-300 placeholder:text-gray-400"
                    placeholder="ejemplo@correo.com"
                  />
                </div>
              </div>

              <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <div>
                  <label
                    for="phone"
                    class="block text-xs font-bold text-gray-400 uppercase tracking-widest mb-2 ml-1"
                    >Teléfono</label
                  >
                  <input
                    type="tel"
                    id="phone"
                    name="phone"
                    class="w-full px-5 py-4 bg-gray-50/50 border border-gray-200 rounded-2xl focus:bg-white focus:ring-4 focus:ring-primary/10 focus:border-primary outline-none transition-all duration-300 placeholder:text-gray-400"
                    placeholder="+34 000 000 000"
                  />
                </div>
                <div>
                  <label
                    for="team"
                    class="block text-xs font-bold text-gray-400 uppercase tracking-widest mb-2 ml-1"
                    >Nombre del Equipo</label
                  >
                  <input
                    type="text"
                    id="team"
                    name="team"
                    class="w-full px-5 py-4 bg-gray-50/50 border border-gray-200 rounded-2xl focus:bg-white focus:ring-4 focus:ring-primary/10 focus:border-primary outline-none transition-all duration-300 placeholder:text-gray-400"
                    placeholder="Tu club o empresa"
                  />
                </div>
              </div>

              <div>
                <label
                  for="message"
                  class="block text-xs font-bold text-gray-400 uppercase tracking-widest mb-2 ml-1"
                  >Mensaje *</label
                >
                <textarea
                  id="message"
                  name="message"
                  rows="4"
                  required
                  class="w-full px-5 py-4 bg-gray-50/50 border border-gray-200 rounded-2xl focus:bg-white focus:ring-4 focus:ring-primary/10 focus:border-primary outline-none transition-all duration-300 placeholder:text-gray-400 resize-none"
                  placeholder="Explícanos brevemente qué necesitas..."
                ></textarea>
              </div>

              <div class="p-6 bg-gray-50 rounded-2xl border border-gray-100">
                <label
                  for="captcha-input"
                  class="block text-xs font-bold text-gray-400 uppercase tracking-widest mb-3 ml-1"
                  >Verificación de Seguridad *</label
                >
                <div class="flex flex-col sm:flex-row items-center gap-4">
                  <div
                    id="captcha-display"
                    class="px-8 py-3 bg-white border-2 border-primary/20 text-3xl font-black tracking-[0.4em] rounded-xl select-none text-primary italic shadow-inner"
                  >
                  </div>
                  <input
                    type="text"
                    id="captcha-input"
                    name="captcha-input"
                    required
                    maxlength="3"
                    pattern="[A-Z]{3}"
                    oninput="this.value = this.value.toUpperCase()"
                    class="flex-1 uppercase px-5 py-3 bg-white border border-gray-200 rounded-xl focus:ring-4 focus:ring-primary/10 focus:border-primary outline-none transition-all text-center font-bold"
                    placeholder="ESCRIBE LAS LETRAS"
                  />
                </div>
              </div>

              <button
                type="submit"
                id="submitButton"
                class="w-full bg-primary hover:bg-primary-dark text-white py-5 px-8 rounded-2xl font-bold transition-all duration-300 flex items-center justify-center space-x-3 shadow-xl shadow-primary/20 hover:shadow-primary/30 hover:-translate-y-1 active:translate-y-0"
              >
                <span>Enviar Solicitud</span>
                <svg
                  class="w-5 h-5"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                  ><path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M14 5l7 7m0 0l-7 7m7-7H3"></path></svg
                >
              </button>
            </form>
          </div>

          <div id="confirmation-message" class="hidden text-center py-12">
            <div
              id="success-message"
              class="hidden bg-green-50 border-2 border-green-200 text-green-800 p-8 rounded-lg"
            >
              <div class="mb-6">
                <div
                  class="mx-auto w-20 h-20 bg-green-100 rounded-full flex items-center justify-center"
                >
                  <svg
                    class="w-10 h-10 text-green-600"
                    fill="none"
                    stroke="currentColor"
                    viewBox="0 0 24 24"
                  >
                    <path
                      stroke-linecap="round"
                      stroke-linejoin="round"
                      stroke-width="3"
                      d="M5 13l4 4L19 7"></path>
                  </svg>
                </div>
              </div>
              <h4 class="text-2xl font-bold mb-3">¡Mensaje Recibido! 🎉</h4>
              <p class="text-lg mb-2">
                Tu solicitud ha sido enviada con éxito.
              </p>
              <p class="text-sm">
                Revisaremos tu información y te contactaremos en un plazo máximo
                de 24 horas.
              </p>
              <p class="mt-4 text-sm font-medium text-green-700">
                Gracias por contactar con Vessport.
              </p>
            </div>

            <div
              id="error-message"
              class="hidden bg-red-50 border-2 border-red-200 text-red-800 p-8 rounded-lg"
            >
              <div class="mb-6">
                <div
                  class="mx-auto w-20 h-20 bg-red-100 rounded-full flex items-center justify-center"
                >
                  <svg
                    class="w-10 h-10 text-red-600"
                    fill="none"
                    stroke="currentColor"
                    viewBox="0 0 24 24"
                  >
                    <path
                      stroke-linecap="round"
                      stroke-linejoin="round"
                      stroke-width="2"
                      d="M6 18L18 6M6 6l12 12"></path>
                  </svg>
                </div>
              </div>
              <h4 class="text-2xl font-bold mb-3">Error al Enviar 😔</h4>
              <p class="mb-2">No pudimos completar tu solicitud.</p>
              <p class="text-sm">
                Por favor, revisa tu conexión e inténtalo de nuevo.
              </p>
              <p class="mt-4 text-sm text-red-700" id="error-details"></p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  // Variables globales
  window.currentCaptcha = '';

  // Generar CAPTCHA
  window.generateCaptcha = function () {
    const captchaDisplay = document.getElementById('captcha-display');
    const captchaInput = document.getElementById('captcha-input');
    const captchaError = document.getElementById('captcha-error');

    const characters = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ';
    let result = '';
    for (let i = 0; i < 3; i++) {
      result += characters.charAt(
        Math.floor(Math.random() * characters.length),
      );
    }
    window.currentCaptcha = result;
    if (captchaDisplay) captchaDisplay.textContent = result;
    if (captchaInput) captchaInput.value = '';
    if (captchaError) captchaError.classList.add('hidden');
  };

  // Inicialización cuando el DOM esté listo
  document.addEventListener('DOMContentLoaded', () => {
    const form = document.getElementById('contactForm');
    const formWrapper = document.getElementById('form-wrapper');
    const confirmationMessage = document.getElementById('confirmation-message');
    const successMessage = document.getElementById('success-message');
    const errorMessage = document.getElementById('error-message');
    const submitButton = document.getElementById('submitButton');

    // Generar CAPTCHA inicial
    window.generateCaptcha();

    // Manejar envío del formulario
    if (form) {
      form.addEventListener('submit', async function (event) {
        event.preventDefault();

        const captchaInput = document.getElementById('captcha-input');
        const captchaError = document.getElementById('captcha-error');
        const isCaptchaValid = captchaInput.value === window.currentCaptcha;

        // Verificar CAPTCHA
        if (!isCaptchaValid) {
          if (captchaError) captchaError.classList.remove('hidden');
          if (captchaInput) captchaInput.value = '';
          window.generateCaptcha();
          return;
        }

        if (captchaError) captchaError.classList.add('hidden');

        // Mostrar estado de carga
        if (submitButton) {
          submitButton.innerHTML = `
            <span>Enviando...</span>
            <svg class="animate-spin h-5 w-5 text-white" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
              <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
              <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8v4l3-3-3-3v4a8 8 0 100 16v-4l-3 3 3 3v-4a8 8 0 01-8-8z"></path>
            </svg>
          `;
          submitButton.disabled = true;
        }

        // Recopilar datos del formulario
        const formData = {
          name: document.getElementById('name')?.value || '',
          email: document.getElementById('email')?.value || '',
          phone: document.getElementById('phone')?.value || '',
          team: document.getElementById('team')?.value || '',
          message: document.getElementById('message')?.value || '',
        };

        try {
          // Construir URL con parámetros (Google Apps Script)
          const url = new URL(
            'https://script.google.com/macros/s/AKfycbwSVF0X7W4Nq43k60oJ0IzA-UCkLybxgWhMR8hMZdTAbUuZeOCGF8iZZt0fXmH5aSQX/exec',
          );

          Object.entries(formData).forEach(([key, value]) => {
            url.searchParams.set(key, value);
          });

          const res = await fetch(url.toString());
          const data = await res.json();

          // Ocultar formulario y mostrar mensaje de éxito
          if (formWrapper) formWrapper.classList.add('hidden');
          if (confirmationMessage)
            confirmationMessage.classList.remove('hidden');
          if (successMessage) successMessage.classList.remove('hidden');
          if (errorMessage) errorMessage.classList.add('hidden');
        } catch (err) {
          console.error('Error al enviar:', err);
          // Mostrar mensaje de error
          if (formWrapper) formWrapper.classList.add('hidden');
          if (confirmationMessage)
            confirmationMessage.classList.remove('hidden');
          if (successMessage) successMessage.classList.add('hidden');
          if (errorMessage) {
            errorMessage.classList.remove('hidden');
            const errorDetails = document.getElementById('error-details');
            if (errorDetails) errorDetails.textContent = err.message;
          }
        } finally {
          if (submitButton) {
            submitButton.innerHTML = `<span>Enviar Solicitud</span><svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3"></path></svg>`;
            submitButton.disabled = false;
          }
          window.generateCaptcha();
        }
      });
    }
  });
</script>
